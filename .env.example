# Salin ke .env lalu isi nilai asli:  cp .env.example .env
# --- PostgreSQL ---
POSTGRES_DB=ternakku
POSTGRES_USER=ternakku
POSTGRES_PASSWORD=ganti_password_kuat_ini

# --- App / integrasi ---
APP_ENV=production
ML_URL=http://ml:8000
REDIS_HOST=redis

# --- Domain (untuk referensi; dipakai saat setup Nginx/TLS & APP_URL Laravel) ---
DOMAIN=ternakku.kaltaraprov.web.id
IP_PUBLIK=103.156.110.104
EMAIL_TLS=spbekaltaradkisp@gmail.com
